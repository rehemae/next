import SignatureCanvas from "react-signature-canvas";
import { useRef } from "react";

const Sign = () => {
  /*   const signPad = useRef(); */
  return <SignatureCanvas canvasProps={{ className: "sigCanvas" }} />;
};

export default Sign;
